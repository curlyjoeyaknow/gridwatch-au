# GridWatch AU — Python runtime dependencies for scripts/
# Used by GitHub Actions (scripts/backfill.py + scripts/materialize.py)
# and for local development.
#
# Install:  pip install -r scripts/requirements.txt
# (The gridwatch package itself is installed separately via: pip install -e .)

# ── OpenElectricity official SDK (replaces raw requests) ─────────────────────
openelectricity>=0.5.0
pandas>=2.0.0

# ── Env vars (reads .env for OPENELECTRICITY_API_KEY) ─────────────────────────
python-dotenv>=1.0.0

# ── Ledger serialization ──────────────────────────────────────────────────────
pyarrow>=15.0.0          # Parquet ledger (ParquetEventLedger)

# ── Materialization ───────────────────────────────────────────────────────────
duckdb>=1.0.0            # SQL engine for aggregations in materialize.py
